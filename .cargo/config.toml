# Windows subsystem configuration is handled by the `windows_subsystem = "windows"` attribute in main.rs.
# No additional linker flags are needed here.
# On Linux, the toolchain is detected automatically.
